[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
buildArgs = { DATABASE_URL = "${{DATABASE_URL}}" }

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
